<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Edgerslottery" >
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <activity android:name=".OrganizerHomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewUserActivity"/>
        <activity android:name=".HomeActivity"/>
        <activity android:name=".OrgHomeActivity"/>
        <activity android:name=".AdminHomeActivity"/>
        <!-- <activity android:name=".LoginActivity"/> -->
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".OrganizerListActivity"/>
        <!--  merge conflict here is resolved-->
        <!--<activity android:name=".OrganizerHomeActivity"/> -->
        <activity android:name=".CreateEditEventActivity"/>
        <activity android:name=".EventDetailsActivity"/>
        <activity android:name=".EventDetailsOrganizer"/>
        <!--  merge conflict here is resolved-->
        <activity android:name=".EventUserChoice" />
        <activity android:name=".EventHistoryActivity" android:exported="false"/>
    </application>

</manifest>